---
name: hh-mcp
description: "Подключить API hh.ru (HeadHunter) к ИИ-ассистенту и работать с вакансиями, откликами и резюме прямо из чата. Ставит MCP-сервер hh-mcp со 133 методами официального API: поиск вакансий, публикация и редактирование, отклики и приглашения, переписка с кандидатами, справочники, статистика зарплат. Используй, когда человек хочет автоматизировать найм, собрать аналитику по вакансиям или разобраться с API hh. Триггеры: «подключи hh», «api hh.ru», «вакансии через api», «headhunter mcp», «отклики кандидатов автоматом», «hh api вакансии», «настрой hh mcp», «выгрузи вакансии с hh»."
---

# hh.ru через MCP

Ставит сервер `hh-mcp` из пакета `hh-mcp-ru`: 133 метода API hh.ru
(чтение 93, запись 31, необратимое 9), каталог собран из официальной спеки
`api.hh.ru/openapi/specification/public`.

## Установка

```bash
uvx hh-mcp-ru
```

Конфиг клиента (Claude Desktop, Cursor, Claude Code):

```json
{
  "mcpServers": {
    "hh": {
      "command": "uvx",
      "args": ["--from", "hh-mcp-ru", "hh-mcp"],
      "env": {
        "HH_TOKEN": "<токен приложения>",
        "HH_APP_NAME": "МоёПриложение (me@example.com)"
      }
    }
  }
}
```

## Где взять токен

1. `dev.hh.ru` → Мои приложения → создать приложение.
2. Скопировать access token.
3. `HH_APP_NAME` заполнить обязательно: hh отклоняет запросы без внятного
   User-Agent, и это первая причина непонятных ошибок 400.

## Что спрашивать у сервера

- `hh_search_methods("отклики на вакансию")` — поиск метода словами.
- `hh_describe_method(...)` — параметры, пагинация, класс доступа.
- `hh_call_method(...)` — прочитать: чтение идёт сразу, без подтверждения.
- `hh_write_method(...)` — записать, нужен `confirm_write`.
- `hh_delete_method(...)` — удалить или изменить необратимо, нужны оба
  подтверждения. Инструмент один на класс доступа: чтение не спрашивает
  разрешения, запись и удаление спрашивают всегда.

## Типовые задачи

- Выгрузить свои вакансии и отклики за период, свести в таблицу.
- Найти вакансии конкурентов по региону и профессии.
- Посмотреть статистику зарплат по роли перед публикацией.
- Ответить кандидатам шаблоном, предварительно показав список человеку.

Не публикуй и не меняй вакансии без явного «да» от человека: это деньги и
репутация работодателя.
